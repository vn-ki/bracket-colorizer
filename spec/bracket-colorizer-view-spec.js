'use babel';

import BracketColorizerView from '../lib/bracket-colorizer-view';

describe('BracketColorizerView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
